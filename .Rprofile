cat("running ladybird-umbrella .Rprofile")

project_loc <- getwd()

pkg_loc <- paste0(project_loc, "/R_Packages")

install_loc <- paste0(project_loc, "/R_Packages/Install")

assign(".lib.loc", install_loc, envir = environment(.libPaths))
